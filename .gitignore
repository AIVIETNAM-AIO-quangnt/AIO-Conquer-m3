# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets (generated by scripts/bootstrap.sh from .env.example)
.env

# Local data cache (PaySim CSV, DuckDB scratch files) -- never committed
data/*
!data/.gitkeep

# Airflow runtime artifacts (bind-mounted into the airflow-* containers) --
# dags/ and plugins/ are source and stay tracked, logs/ is not
airflow/logs/

# local infrastructure (debugging only)
docker/mlflow*
compose.*.yaml

# AI agent & IDE
.vscode
CLAUDE.md
AGENTS.md
skills-lock.json
.agents
.mcp.json
.claude
.codegraph
.rtk
.hypothesis
.lavish